# Production Dockerfile for Static Frontend

FROM nginx:alpine

# Copy static assets into Nginx standard html folder
COPY frontend/ /usr/share/nginx/html/

# Expose HTTP port 80
EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
